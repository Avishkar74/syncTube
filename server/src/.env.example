# syncTube Server - Example Env Vars
# Copy this file to .env and adjust values for your environment

NODE_ENV=production
# Locally you can use 3001. On Render, the platform provides PORT automatically.
PORT=3001

# MongoDB Atlas connection string
MONGODB_URI=mongodb+srv://<user>:<password>@<cluster>/<db>?retryWrites=true&w=majority

# Comma-separated allowed origins for REST and Socket.IO
# Include localhost for dev, your Netlify site, and this Render URL
CLIENT_ORIGINS=http://localhost:5173,https://synctubeee.netlify.app,https://synctube-8veb.onrender.com
